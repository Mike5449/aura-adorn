# -----------------------------------------------------------------------
# Copy this file to .env and fill in your values.
# NEVER commit .env to source control.
# -----------------------------------------------------------------------

# PostgreSQL connection
DATABASE_URL=postgresql://postgres:CHANGE_ME@localhost/app_base_system

# JWT secrets — generate with:  python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=CHANGE_ME_generate_with_secrets_token_hex_32
REFRESH_SECRET_KEY=CHANGE_ME_generate_with_secrets_token_hex_32_different_value

# JWT algorithm (keep HS256 unless you switch to RS256)
ALGORITHM=HS256

# Token lifetimes
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# CORS — comma-separated list of allowed frontend origins
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# Brute-force protection
MAX_LOGIN_ATTEMPTS=5
ACCOUNT_LOCKOUT_MINUTES=15

# MonCash (Digicel mobile money) — get credentials from
# https://moncashbutton.digicelgroup.com/Moncash-business
# Use the sandbox until ready for live transactions.
MONCASH_CLIENT_ID=
MONCASH_CLIENT_SECRET=
MONCASH_MODE=sandbox          # sandbox | production
MONCASH_RETURN_URL=http://localhost:5173/checkout/return
